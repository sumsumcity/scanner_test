<%- include('./adminHeader.ejs')%>


<%- include('./navigationBar.ejs') %>


<script type="text/javascript" src="/javascripts/deleteUser.js"></script>

<div class="col-sm-12" >
    <div class="container-fluid">
        <div class="table-responsive">
            <table class="table table-hover table-bordered" >
                <thead>
                <tr>
                    <% Object.keys(data).forEach(function(key) { %>
                        <th><%= key %></th>
                    <% }); %>
                    <th>Edit</th> <!-- Add a new column for the delete button -->
                    <th>Delete</th>
                </tr>
                </thead>
                <tbody>
                <% if (Object.values(data)[0]) { %>
                    <% for(let i = 0; i < Object.values(data)[0].length; i++) { %>
                        <tr>
                            <% Object.keys(data).forEach(function(key) { %>
                                <td><%= data[key][i] %></td>
                            <% }); %>
                                <td>
                                    <% if (data['email'][i] !== adminEmail) { %>

                                        <a href="/a0MNAc/user/<%= data['_id'][i] %>" class="btn btn-secondary">Edit</a>

                                    <% } %>
                                </td>
                                <td>
                                    <% if (data['email'][i] !== adminEmail) { %>
                                        <button type="button" class="btn btn-danger open-delete-modal" data-toggle="modal" data-id="<%= data['_id'][i] %>">
                                            Delete
                                        </button>
                                    <% } %>
                                </td>
                        </tr>
                    <% } %>
                <% } %>
                </tbody>
            </table>
        </div>
    </div>
</div>


<!-- Modal -->

<div class="modal fade" id="deleteModal" tabindex="-1" role="dialog" aria-labelledby="deleteModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="deleteModalLabel">Delete User</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">×</span>
                </button>
            </div>
            <div class="modal-body">
                Are you sure you want to delete this user?
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
                <button type="button" class="btn btn-primary delete-btn">Delete</button>
            </div>
        </div>
    </div>
</div>







</html>






